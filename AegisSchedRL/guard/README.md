## Guard Module (Module 3)

Implements Section 4.5 and Algorithm 3:

- Capacity feasibility check (Eq. 14)
- Deadline admissibility check (Eq. 15)
- Latency-aware edge preference (Eq. 16)
- Fallback selection from feasible set (Eq. 17)

`HybridGuard.validate_or_fallback(...)` is the main entry point.

